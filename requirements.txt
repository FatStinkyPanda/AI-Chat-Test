# Core dependencies for Brain-Inspired Conversational AI
# Minimal set for quick installation on Python 3.14

# Semantic processing and embeddings
sentence-transformers>=2.2.0

# Scientific computing (use already installed versions)
scikit-learn>=1.3.0

# Note: These should already be satisfied by sentence-transformers
# - torch>=2.0.0
# - transformers>=4.30.0
# - numpy>=1.24.0
# - pydantic>=2.0.0

# Vector database - Install separately if needed
# chromadb has compatibility issues with Python 3.14
# Install manually: pip install chromadb==0.5.5 --no-deps
# Then: pip install httpx hnswlib opentelemetry-api opentelemetry-sdk

# Optional dependencies (uncomment to enable)
# Audio I/O
# SpeechRecognition>=3.10.0
# pyaudio>=0.2.13
# pyttsx3>=2.90

# Video I/O
# opencv-python>=4.8.0

# Web interface
# fastapi>=0.100.0
# uvicorn>=0.23.0
# websockets>=11.0
